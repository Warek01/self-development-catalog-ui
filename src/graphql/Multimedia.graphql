fragment multimediaFields on UploadFileEntityResponse {
  data {
    id
    attributes {
      alternativeText
      url
      width
      height
      caption
      formats
      mime
      name
      previewUrl
      size
      ext
    }
  }
}
